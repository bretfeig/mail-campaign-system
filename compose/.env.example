MODE=test

# Parser (IMAP disabled in test; the parser will read /data/samples .eml files)
IMAP_HOST=
IMAP_PORT=993
IMAP_USER=
IMAP_PASS=
IMAP_SSL=true

# LLM (disabled by default)
LLM_PROVIDER=none
LLM_MODEL=
LLM_API_KEY=
LLM_ENDPOINT=

# In production, Listmonk SMTP will be configured via Listmonk UI to either
# Mailpit (test) or Mailcow submission (prod). No secrets here.
